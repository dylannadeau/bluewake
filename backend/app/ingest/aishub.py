"""AISHub ingestion (stub — Phase 1).

Plan: periodic poll of the AISHub member API (requires contributing a feed or
a granted account). Same output contract as aisstream: ais_positions rows plus
identity attributes forwarded to resolution.
"""
